package com.product.school.repositories;

import com.product.school.data.GeneralSubjects;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeneralSubjectRepository extends JpaRepository<GeneralSubjects, Long> {
}
